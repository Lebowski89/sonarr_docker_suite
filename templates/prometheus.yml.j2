global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: 'prometheus'
    scrape_interval: 5s
    static_configs:
      - targets: ['{{ lookup("vars", sonarr_prefix + "_prometheus_basics_name") }}:{{ lookup("vars", sonarr_prefix + "_prometheus_ports_cont") }}']