---

sonarr_recyclarr_is_enabled: '{{ (sonarr_recyclarr_env_timezone is defined) and
                                 (sonarr_recyclarr_env_timezone is not none) and
                                 (sonarr_recyclarr_env_timezone | trim | length > 0) and
                                 (sonarr_recyclarr_env_puid is defined) and
                                 (sonarr_recyclarr_env_puid is not none) and
                                 (sonarr_recyclarr_env_puid | trim | length > 0) and
                                 (sonarr_recyclarr_env_pgid is defined) and
                                 (sonarr_recyclarr_env_pgid is not none) and
                                 (sonarr_recyclarr_env_pgid | trim | length > 0) }}'
